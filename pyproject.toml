[project]
name = "mysteries-life-sim"
version = "0.22.0"
description = "V0.22 rule-driven life simulator in a mystery-world setting (Tingen, 1348)."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
